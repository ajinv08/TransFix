package oOPSConcept;

public interface InterfaceConcept  {


	// class vs abstract class vs interface
	
	// interface all methods are only ABSTRACT
	
	// no class can inherit interface but vice versa is possible key word is "implements"

	// interface to interface can be inherited
	
	void test1(); // by default its abstract. so no need of defining as abstract explitily
	abstract void test2(); // you can give abstract key word if you need
	
	
	
}
